node_modules

# Variables de entorno locales
.env
.env.local

# Salida del build. Se compila en el VPS, no se versiona: tenerla en git
# chocaba con el dist que genera el propio servidor en cada pull.
dist/

# Tiles del mapa: las genera TsMap, no se versionan (~92 MB, 21845 archivos)
public/map/Tiles/
